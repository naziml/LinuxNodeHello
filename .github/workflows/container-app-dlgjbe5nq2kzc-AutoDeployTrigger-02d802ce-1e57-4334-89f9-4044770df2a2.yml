name: Trigger auto deployment for container-app-dlgjbe5nq2kzc

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '/bin/**'
    - '.github/workflows/container-app-dlgjbe5nq2kzc-AutoDeployTrigger-02d802ce-1e57-4334-89f9-4044770df2a2.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPDLGJBE5NQ2KZC_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPDLGJBE5NQ2KZC_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPDLGJBE5NQ2KZC_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/bin 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPDLGJBE5NQ2KZC_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPDLGJBE5NQ2KZC_REGISTRY_PASSWORD }}
          containerAppName: container-app-dlgjbe5nq2kzc
          resourceGroup: linuxnodehello8e83
          imageToBuild: default/container-app-dlgjbe5nq2kzc:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


