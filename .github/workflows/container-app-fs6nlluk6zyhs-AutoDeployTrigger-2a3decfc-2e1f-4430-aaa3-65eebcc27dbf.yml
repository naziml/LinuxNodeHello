name: Trigger auto deployment for container-app-fs6nlluk6zyhs

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-fs6nlluk6zyhs-AutoDeployTrigger-2a3decfc-2e1f-4430-aaa3-65eebcc27dbf.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPFS6NLLUK6ZYHS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPFS6NLLUK6ZYHS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPFS6NLLUK6ZYHS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPFS6NLLUK6ZYHS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPFS6NLLUK6ZYHS_REGISTRY_PASSWORD }}
          containerAppName: container-app-fs6nlluk6zyhs
          resourceGroup: linuxnodehello9110
          imageToBuild: default/container-app-fs6nlluk6zyhs:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


